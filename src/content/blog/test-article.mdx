---
title: Shader Test
summary: Testing live GLSL shader
date: 2026-01-01
---

import ShaderPlayground from "../../components/ShaderPlayground.astro";
import CodeBlock from "../../components/CodeBlock.astro";

# Live shader

```glsl live
#ifdef GL_ES
precision mediump float;
#endif

uniform vec2 u_resolution;
uniform float u_time;

void main(){

vec2 uv = gl_FragCoord.xy / u_resolution;

vec3 col = 0.5 + 0.5 * sin(u_time + uv.xyx + vec3(0.0,2.0,4.0));

gl_FragColor = vec4(col,1.0);

}
```

## Exemple Python

<CodeBlock
lang="python"
code={`
import math

def noise(x):
return math.sin(x) _ 0.5 + math.sin(2 _ x) \* 0.25
`}
/>

## Exemple C#

<CodeBlock
    lang="csharp"
    code={`
public class Player
{
    public float speed = 5.0f;
}
`}
/>
